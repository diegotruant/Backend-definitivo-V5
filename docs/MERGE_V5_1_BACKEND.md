# Backend-definitivo-V5.1 merge

Build locale unificata con:

- fix `/ride/summary` per FIT lunghi tramite HRV/DFA-alpha1 adattivo;
- hardening/stress tests;
- Canonical TwinState v1;
- season projection / what-if;
- neuromuscular profile;
- power-source normalization;
- manual non-cycling load injection;
- endpoint FastAPI integrati.

Questa build mantiene il nome progetto `Backend-definitivo-V5`; `5.1.0` indica l'incremento funzionale sopra V5, non una nuova generazione del backend.
