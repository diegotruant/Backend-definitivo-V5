# scripts/

Utility scripts live here as the repository evolves.

## Entry-point CLI tools (root-level, registered in pyproject.toml)

These two scripts live in the repo root because they are registered as
installable CLI entry points (`pip install -e .`):

| Script | Entry point | Purpose |
|---|---|---|
| `run_in_person_test.py` | `run-in-person-test` | Drive in-person field-testing protocols |
| `verify_package_integrity.py` | `verify-package-integrity` | Sanity-check the installed package |

## Test suites

| Suite | How to run | Purpose |
|---|---|---|
| `tests/pytest_smoke.py` | `make test` | Quick smoke — package imports only |
| `make test-all` | `pytest tests/pytest_*.py pytest_script_suite.py` | Unit tests + 25 integration scripts + golden regression |
| `pytest_script_suite.py` | (included in `make test-all`) | Runs every `tests/integration/test_*.py` as a subprocess |

Integration scripts under `tests/integration/` are **not** plain `def test_*` functions;
they are executable regression scripts collected by `pytest_script_suite.py`.
CI runs `make test-all` on every push.

## Analysis / stress scripts (under `tools/`)

| Script | Location | Purpose |
|---|---|---|
| `analyze_synthetic_fit_dataset.py` | `tools/analysis/` | Batch analytics over synthetic FIT data |
| `analyze_uploaded_fit_archives.py` | `tools/analysis/` | Inspect uploaded FIT archives |
| `analyze_uploaded_fit_full_engine_audit.py` | `tools/analysis/` | Full per-engine FIT audit |
| `comprehensive_stress_test.py` | `tools/stress/` | Engine regression stress suite |
| `validate_on_real_fits.py` | `tools/stress/` | Real FIT classification checks |
| `standalone_adaptive_load_api.py` | `tools/` | Dev-only adaptive-load API prototype (not production entry point) |

## OpenAPI / contract

| Script | Purpose |
|---|---|
| `export_openapi.py` | Regenerate `openapi/openapi.json` (`make openapi-frontend`) |

## Athlete model (post-deploy)

| Script | Purpose |
|---|---|
| `smoke_test_athlete_model.sql` | Supabase SQL checks after migrations 004–006 |
| `../tools/worker/ingest_athlete_model_pipeline.py` | Reference worker: FIT → MMP → profile → twin |

See `docs/ATHLETE_MODEL_DEPLOY_CHECKLIST.md`.

`reports/` and `frontend/public/data/` are build artefacts generated by the
`tools/analysis/analyze_*.py` scripts.  They are excluded from git (see `.gitignore`).
`synthetic_fit_dataset.tar.gz` is likewise excluded — regenerate it locally with
the dataset-generation script before running `tools/analysis/analyze_synthetic_fit_dataset.py`.
